# Instant factorial – no recursion needed!
function factorial(n) {
    set result to 1
    while n > 0 do
        set result to result * n
        set n to n - 1
    end
    return result
}
print factorial(5)